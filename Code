#include "mbed.h"
#include "LM75B.h"
#include "C12832.h"

LM75B sensor(p28,p27);
C12832 lcd(p5, p7, p6, p8, p11);


int main() {
    while(1) 
    {
        lcd.cls();
        lcd.locate(0, 18)
        lcd.printf("Temperature = %.3f\n", (float)sensor;
        wait(1.0) 
    }
}
